package main

/*
	this version should rewrite the intersection without the use of separate
	functions to find min and max, and should find all points

	additionally it is worth using the given bundle to eliminate the need to
	find both since we already have one of the two (under strict convexity/IDs)
*/

func B(x float64) float64 {
	// budget set
}

func I(x float64) float64 {
	// indifference set
}

func findIntersection(x float64) float64 {
	// find the other bundle that exists along the intersection of I(x) and B(x)
}

// not too sure how I want to break down different optimization objects

type hicks struct {
	// objective and constraint
}

type marshall struct {
	// objective and constraint
}

func main() {

}
